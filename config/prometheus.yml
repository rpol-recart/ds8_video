global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "container-detector"
    static_configs:
      - targets: ["container-detector:8000"]
    metrics_path: /metrics
